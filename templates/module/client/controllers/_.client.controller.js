'use strict';

define(['angular'], function (angular) {
    // Articles controller
    angular.module('sales').controller('SalesController', ['$scope', '$stateParams', '$location', 'Authentication',
  function ($scope, $stateParams, $location, Authentication) {}
]);
});
